name: CI

on:
  push:
  workflow_dispatch:

jobs:
  lint:
    uses: ./.github/workflows/_lint.yml
  run-tests:
    uses: ./.github/workflows/_tests.yml
    secrets:
      ROBLOSECURITY: ${{ secrets.ROBLOSECURITY }}
